{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
{#    Avec Submit button depuis le FormType#}
    {%  if app.user %}
        <p>{{ app.user.email }}</p>
    {% endif %}

    {{ form(formCourse) }}


{#    Sans Submit button depuis le FormType #}
{#    {{ form_start(formCourse) }}#}
{#        {{ form_widget(formCourse) }}#}
{#        <button>Créer le cours</button>#}
{#    {{ form_end(formCourse) }}#}
{% endblock %}
